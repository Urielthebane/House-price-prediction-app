#Run the following command in your terminal (where you see the (venv)):

PowerShell

pip install pandas

pip install pandas fastapi uvicorn joblib jinja2
How to verify

#After the installation finishes, you can type:


pip list

You should see pandas (and the others) in the list. Then, try running your app again:
then type in:

 Python app/app.py

to activate the app